/**
 * Base URL of the Stoa backend. The mobile app always talks to a deployed
 * instance (the phone can't see your dev machine's localhost without extra
 * setup), so point this at Railway.
 *
 * TODO: replace with your real Railway URL, e.g. "https://stoa-production-xxxx.up.railway.app"
 */
export const API_BASE = "https://a-stoic-mind-production.up.railway.app";
